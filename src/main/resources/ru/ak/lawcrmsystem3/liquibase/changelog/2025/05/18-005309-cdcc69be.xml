<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd"
        objectQuotingStrategy="QUOTE_ONLY_RESERVED_WORDS">

    <changeSet id="alter-email-sending-attachment-content-id-length" author="LawCrmSystem3">
        <comment>Increase length of CONTENT_ID column in EMAIL_SENDING_ATTACHMENT table</comment>
        <sql dbms="postgresql">
            ALTER TABLE EMAIL_SENDING_ATTACHMENT
            ALTER COLUMN CONTENT_ID TYPE VARCHAR(255);
        </sql>
        <rollback>
            -- SQL to revert the change if needed (careful with data truncation if values exceed 50)
            <sql dbms="postgresql">
                ALTER TABLE EMAIL_SENDING_ATTACHMENT
                ALTER COLUMN CONTENT_ID TYPE VARCHAR(50);
            </sql>
        </rollback>
    </changeSet>

</databaseChangeLog>